export const jwtConstants = {
  secret: 'CHANGE IT LATER',
};

export enum AuthApiTags {
  API_TAG = 'Auth',
}

export enum AuthApi {
  CONTROLLER = 'auth',
}

export enum AuthApiPath {
  LOG_IN = 'logIn',
}
